using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyPush : MonoBehaviour
{
    [Header("‘Î‰ž‚µ‚Ä‚¢‚éKey")]
    [Tooltip("‘Î‰ž‚µ‚Ä‚¢‚éKey")] public KeyCode _key;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(_key))

    }
}
